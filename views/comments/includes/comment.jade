.comment(id=comment._id.toString())
  .content.textHtml!= comment.html
  .meta
    a(href="/articles/"+comment.article._id+"/"+comment.article.slug+"#"+comment._id)
      time(title=__('Дата публикации комментария: ') + moment(comment.createdAt).calendar() datetime=moment(comment.createdAt).format())= moment(comment.createdAt).fromNow()
    = " · "
    span.author
      a(href="/users/"+comment.creator.username title=__('Автор комментария: ') + comment.creator.username)= comment.creator.username